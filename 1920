class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        vector<int> a;
        for(int t=0;t<nums.size();t++)
            a.push_back(nums[nums[t]]);
        return a;
    }
};